Naslednja opravila so razvrscena po prioriteti (zgoraj bolj nujna):

- (M) Popravi urls.py in tomo/urls.py, kateri je ta pravi? Kaj je treba includati iz settings.py?

- Ctrl-M-ji se pojavijo vsakič, ko editiram nalogo preko djangoadmin (MacOS, Chrome).

- (M) USER INTERFACE:
  - če ima naloga samo en part, ni treba, da se ta part označi z "1)".
  - ali bi spremenili "1), 2), 3)" v kaj drugega?
  - gumbi pri nalogi: Kopiraj, Premakni gor, Premakni dol,

- (M) naročnje na predmete pri študentih in učiteljih, pod spiskom predmetov

- koristno bi bilo, ce bi lahko ucitelj komentiral resitve studentov,
  in obratno, da lahko student kontaktira (koga, avtorja?)

- STATISTIKA:
  - overall statistika
  - statistika po času
  - statistika samo za eno nalogo

- kopiranje problem-setov med predmeti in kopiranje nalog med problem-seti

- (A) avtomatičen replace za študente:

    primer:

    def backup_name(filename):
        name, extension = os.path.splitext(filename)
        index = 1
        while os.path.exists('{0}-{1}{2}'.format(name, index, extension)):
            index += 1
        return '{0}-{1}{2}'.format(name, index, extension)

    filename = os.path.abspath(sys.argv[0])
    shutil.copy2(filename, backup_name(filename))
    with open(filename, "wb") as f:
        f.write(new_contents)

- avtomaticen replace za R

- narediti Check.compare in Check.run odporna na izjeme.

- narediti grupo "ucitelj", da bomo lahko locili administratorje od uciteljev

- podpora za Mathematico

- podpora za matlab

- podpora za Haskell

- podpora za Ocaml

- podpora za Javo
